# PLAN.md — 実装計画

最終更新: 2026-07-29

開発方針は `CLAUDE.md` を正とする。本書は「どこまで終わったか」だけを持つ。
決定事項と発見は `NOTES.md` に追記する。

---

## 目的

農地法5条の許可申請に添付する計画平面図を、縮尺1/250のベクタPDFとして出力できるようにする。

---

## 設計の骨格

**縮尺の真実は1箇所。** `src/paper/transform.ts` に `metersToPaperMm(m) = m * 1000 / scale` だけを置き、
これを読むのはPDF生成層のみとする。MapLibreのビュー状態がここに流れ込む経路を作らない。
1/250では 1m = 4mm、20m = 80.0mm = 226.772pt。

**内部座標は平面直角座標（メートル）に統一。** 平面直角座標は X=北・Y=東なので、
紙面では `紙X = Y - originY`、`紙Y = X - originX` と入れ替わる。取り違えやすいので単体テストで固定する。

**用紙は「動かす紙」。** 図郭左下隅に対応する平面直角座標 `paperOrigin` を1点だけ持つ。
画面のドラッグはこれを更新する。ズームは `paperOrigin` に触れない。

### モジュール構成（予定）

```
src/
  geo/
    crs.ts        系番号→proj4定義、toXY / toLonLat
    area.ts       座標法（倍面積）、閉合誤差
    polygon.ts    面積・重心・外周長
  data/
    moj/          登記所備付地図GeoJSONの読み込み・正規化・精度区分判定
    project.ts    プロジェクトJSONスキーマ + 入出力 + バージョン
  paper/
    layout.ts     用紙定義(A3/A2/A1)、図郭、綴じ代、mm↔pt
    transform.ts  平面直角座標(m) → 紙上mm  ← 縮尺の唯一の定義
  pdf/
    doc.ts        pdf-libドキュメント生成、フォント埋め込み
    frame.ts      二重図枠、表題枠、方位記号、表題欄
    draw.ts       筆界・道路・水路・建物・フェンス・車
    symbols.ts    車SVGパス、フェンス玉鎖、土留めT字、桝、矢印
    sekiseki.ts   求積図・求積表（別紙）
  ui/
    MapView.tsx      MapLibre + 筆の選択
    PaperFrame.tsx   用紙枠のドラッグ配置
    LabelEditor.tsx  ラベルの手動配置
```

---

## 手順

### 第0段階 — 縮尺とフォントの実証

- [x] Vite + React + TS + Vitest の骨格を作る　[検証: `npm run build` と `npm test` が通る]
- [x] `paper/layout.ts` に mm↔pt と A3/A2/A1 の定義　[検証: 単体テスト 1mm = 2.834645669pt]
- [x] `paper/transform.ts` に縮尺変換　[検証: 単体テスト 20m → 80.0mm]
- [x] 日本語フォントのサブセット埋め込み　[検証: 生成PDFに「計画平面図」が文字化けせず出る]
- [x] 20m正方形＋図枠＋表題＋方位＋表題欄のA3横PDFを出力　[検証: コンテンツストリーム解析で当該辺が226.772pt±0.01]
- [x] 100mm/200mmの基準線入り検証用PDFを出力　[検証: 同上]
- [ ] **確認ゲート** 依頼者が印刷して定規で実測　[検証: 20mの辺が80.0±0.5mm。検収基準1]

### 第1段階 — 地図データの取り込みと敷地の確定

実データが要る項目がある（下記★）。設計と実装はブロックされない。

- [ ] `geo/crs.ts` 系番号→proj4定義　[検証: 国土地理院の公表値との固定値テスト。検収基準3]
- [ ] `geo/zone.ts` 緯度経度→系番号の推定　[検証: 各系の代表地点で正しい系が返る固定値テスト]
- [ ] `geo/area.ts` 座標法と閉合誤差　[検証: 20m正方形が400.00㎡。検収基準2]
- [ ] 住所検索（地理院API）と地図ビューの移動　[検証: 住所を入れて地図が動き、推定系番号が画面に出る]
- [ ] ★ 対象市町村のGeoJSONを1本入手し、プロパティ名と精度区分の表現を確認する　[検証: 実物のキー一覧を NOTES.md に記録]
- [ ] ★ GeoJSON読み込みと筆界・地番の地図表示　[検証: 既知の地番が正しい位置に出る。読み込み筆数を報告。検収基準5]
- [ ] 14条地図と公図の区分判定と画面表示　[検証: 判定結果が筆ごとに出る]
- [ ] 申請地の複数筆選択、筆ごとの地目・面積・所有者の保持　[検証: 2筆選択してJSONに反映される]
- [ ] 辺長の手入力による上書き　[検証: 上書き後の面積が変わる]
- [ ] 閉合誤差の表示　[検証: 意図的に閉合しない辺長で誤差が数値表示され、無警告で通らない。検収基準4]
- [ ] クリックによる概形作図のフォールバック
- [ ] プロジェクトJSONの入出力　[検証: 往復して同一になる]

### 第2段階 — PDF出力（計画平面図）

- [ ] 図枠・表題・方位・表題欄をテンプレートとして分離　[検証: 敷地データ無しで図枠だけ出せる]
- [ ] 筆界・道路・水路・建物の描画　[検証: 参考図面と線種が一致]
- [ ] フェンス（玉鎖線）と土留め（外向きT字）の記号生成　[検証: 目視]
- [ ] 用紙枠のドラッグ配置UI　[検証: ズームしても出力PDFが変わらない]
- [ ] 地番・道路名・水路名ラベルの手動配置　[検証: 位置がJSONに保存される]
- [ ] 表題欄の入力フォームと描画　[検証: 入力した文字が右下の枠に出る]
- [ ] 用紙に収まらない場合の警告とA2/A1提案　[検証: 100m超の敷地で警告が出る]
- [ ] **参考図面との記載要素の突合表を提出**　[検収基準6]

### 第3段階 — 駐車区画の自動割付

- [ ] 車の平面シンボルSVGパスを参考図面の輪郭から起こす　[検証: 目視で参考図面と並べる]
- [ ] 区画パラメータ（2.5×5.0m、車路5.5m）と自動配置　[検証: 参考図面と同等の敷地で同程度の台数]
- [ ] 手による微調整　[検証: 位置がJSONに保存される]
- [ ] 台数の画面表示（図面には載せない）

### 第4段階 — 付帯施設と排水

- [ ] 進入口、隅切り
- [ ] 排水経路と放流先の矢印
- [ ] 集水桝・側溝の記号

### デプロイ

- [ ] Vercelにデプロイ
- [ ] **Deployment Protection を有効化**　[検証: 未認証のブラウザで公開URLが開けないことを確認し、報告に含める]

---

## 当面やらないこと

求積図と求積表は作らない。面積計算そのものは計画平面図に筆ごとの面積を記入するために残す。

## 残る不明点

検収基準5に使う実データ（対象市町村の登記所備付地図GeoJSON）が1本必要。設計はブロックしない。
