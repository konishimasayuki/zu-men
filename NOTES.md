# NOTES.md — 確定事項と発見

新しい事実が確定したら1行ずつ追記する。恒久的な規約になったものは `CLAUDE.md` へ昇格させる。

---

## 2026-07-29 — 参考図面の読み取り

`reference/IMG_9055.png` は撮影時に90°回転している。読むときは反時計回りに90°戻す。
正しい向きは A3横、綴じ穴は紙の左端。

CLAUDE.md の旧「参考図面から読み取った要件」のうち体裁の記述は、回転した写真の誤読だった。
訂正済み。訂正の内訳は次のとおり。

| 項目 | 旧記述（誤） | 実物 |
|---|---|---|
| 表題 | 右端に縦書き | 上辺中央やや右に横書き |
| 方位記号 | 右上 | 左上 |
| 綴じ代 | 上辺 | 左端（2穴は紙のパンチ穴で作図要素ではない） |
| 出典表示 | 必ず入れる | 参考図面には無い。地図画像を使っていないため |
| 表題欄 | 記述なし | 右下に空欄の枠が1つある |

### 参考図面の実測値

- 申請地は2筆。**44-4（田・590・田中辰弘）** と **90-1（田・747）**。
  - 面積の単位は㎡と推測。図上計測で敷地の外接矩形が約37m×39mとなり、590+747=1337㎡ とおおむね整合する。
  - 「590」の末尾桁は手書きが潰れており判読は確実でない。
  - 90-1 には所有者名が書かれていない。
- 隣接地番は 32・89・91。地番のみで、地目・面積・所有者は書かれていない。
- 車は約23台（写真からの目視計数、±1）。
- 注記は「市道」4箇所、「用悪水路」3箇所、「通路」2箇所。
- 隣家（91）は建物輪郭にケバ状のハッチ。
- 敷地外周は玉鎖線（フェンス）。南側と東側の外側に外向きT字記号（土留め）。
- 敷地の南西角外側に菱形の記号が1つ。集水桝とみられる。
- 寸法線なし。台数の記載なし。凡例なし。求積表なし。
- 進入口は市道側に手書きの「通路」引出し線が1箇所あるのみ。明確な矢印記号は判別できなかった。
- 地番・地目・面積・所有者は印刷ではなく手書きで加筆されている。本アプリでは印字する。

---

## 2026-07-29 — 決定

- 図面の体裁は参考図面の実物に合わせる。
- PDFはベクタ線画のみ。地理院タイルは画面編集の下敷き専用で、PDFには焼き込まない。出典表示は不要。
- 対象地は14条地図の区域。ただし求積は地積測量図の実測値で確定する方針は変えない。
- 寸法線を入れない。台数を図面に記載しない。
- 実装順序は第0段階（縮尺・フォントの実証）を先に置く。

---

## 技術メモ

- 1/250 では 1m = 4mm。20m = 80.0mm = 226.772pt（1mm = 2.834645669pt）。
- 平面直角座標は X=北・Y=東。紙面座標とは軸が入れ替わる。
- pdf-lib の標準フォントに日本語はない。@pdf-lib/fontkit でサブセット埋め込みが必要。
  所有者名に任意の漢字が来るためビルド時の事前サブセットは効かない。フォント実体4〜8MBを同梱する。
- pdf-lib の `drawSvgPath` は y軸下向き。車シンボルの配置で符号に注意。

---

## 2026-07-29 — 用紙をA4に訂正

依頼者のプリンタは**A4**。旧CLAUDE.mdのA3既定はClaude側が根拠なく定めたものだった。
依頼者はA3にこだわっていない。既定をA4横に変更した。

| 用紙 | 図郭[mm] | 1/250での実寸 |
|---|---|---|
| A4横 | 262 × 180 | 65.50 m × 45.00 m |
| A3横 | 385 × 267 | 96.25 m × 66.75 m |
| A2横 | 559 × 390 | 139.75 m × 97.50 m |
| A1横 | 806 × 559 | 201.50 m × 139.75 m |

対象2件（約330㎡・約496㎡）の敷地そのものはA4に余裕で収まる。
330㎡なら約18m四方、496㎡なら約22m四方で、紙上ではそれぞれ73mm・89mm。
問題は周辺の描画余地で、22m四方の敷地をA4中央に置くと左右21m・上下11mしか残らない。
参考図面は約96m×66mの範囲を描いているので、同じ密度では入らない。

**縮小印刷で辻褄を合わせてはならない。** 制約2として明文化した。

### この訂正で見つかった不具合

検証シートの注意書きに「A3で印刷し」が固定文字列で埋め込まれていた。
A4を選ぶと「A4なのにA3で印刷しろ」と書かれた紙が出る状態だった。
`buildCalibrationNote(size)` に切り出し、用紙名が一致することをテストで固定した。

---

## 2026-07-29 — 表題欄の削除と印刷補正

### 表題欄

右下の表題欄（会社名等の記入欄）は不要との指示により削除した。
参考図面には空欄の枠があるが、本アプリでは描かない。
前回「ユーザーに入力させる」と決めた方針を取り消したもの。復活させる場合は
git 履歴の `drawTitleBlock` を参照。

### 印刷補正（キャリブレーション）

依頼者がいま印刷できる環境にないため、検収基準1の実測は保留。
あとから実測値を入れて調整できるよう印刷補正を実装した。

- 検証シートの200mm線を定規で測り、実測値を入力すると倍率 = 基準長 / 実測値 を計算する。
- 補正はページ内容の先頭に `cm` 演算子1つとしてだけ置く。用紙の中心を固定して拡大縮小する。
- **用紙寸法（MediaBox）には倍率を掛けない。** 掛けるとプリンタ側の扱いが変わる。
- 許容範囲は ±5%。それを超える食い違いは設定ミス（「用紙に合わせる」が有効等）なので拒否する。
  補正で設定ミスを隠さない。
- 設定は localStorage に保存し、次回起動時も効かせる。

実測（`npx vite-node scripts/calcheck.ts`）:

| 200mm線の実測値 | 補正倍率 | 20mの辺の描画長 | そのプリンタで刷った結果 |
|---|---|---|---|
| 200.0mm | 1.00000 | 80.0000mm | 80.0000mm |
| 198.0mm | 1.01010 | 80.8081mm | 80.0000mm |
| 202.0mm | 0.99010 | 79.2079mm | 80.0000mm |
| 195.5mm | 1.02302 | 81.8414mm | 80.0000mm |

**補正を有効にしたPDFはデータとしては1/250でなくなる。** 紙に出したときに1/250になる。
電子提出する場合は補正を無効にすること。UIにもこの旨を表示している。

### 測定ツール側の修正

PDF解析器が座標を生のまま読んでいたため、`cm` による拡大縮小を見逃す状態だった。
「80mmと書いてあるが実際は80.8mm」を通してしまう。CTM（座標変換行列）を
q / Q のスタックごと追跡し、変換後の実効長を測るように直した。

---

## 2026-07-29 — 第1段階（座標変換・住所検索・地図ビュー）

### 座標変換（検収基準3）

系のパラメータは記憶で書かず、PROJ 9.5.1 のEPSGデータベース（EPSG:6669〜6687）から
取り出した。`scripts/genCrsFixtures.py` が同じデータベースから検証用の固定値を作る。
自分の実装を自分の記憶で検証しても意味がないので、独立実装と突き合わせる。

- 19系すべての原点 → (0,0) の最大ずれ: 7.08e-10 m
- 実在9地点での proj4js と PROJ の最大差: **5.45e-4 mm**
- 系を9→8に取り違えた場合のずれ: **120.1 km**（確認UIが要る理由）

**XIII系の原点は東経144度15分。** 143度30分ではない。記憶で書いていたら間違えていた。

**軸の順序。** 平面直角座標系は X=北・Y=東 だが proj4 の出力は [easting, northing]。
`crs.ts` が入れ替えを吸収し、外にはX=北・Y=東で渡す。

### 系番号の推定

測量法施行令第9条の適用区域は都道府県または市町村単位。
1つの系に収まる43都府県は都道府県名だけで確定でき、confidence は `certain`。
北海道・東京都・鹿児島県・沖縄県は複数系にまたがるため緯度経度で近似し、`approximate` を返す。

**北海道の近似は日高振興局で外れる。** 浦河町は第XI系だが経度142.77度なので
近似規則では第XII系になる。誤りを隠さずテストで固定し、UIで必ず確認させる。

### 地理院のエンドポイント（実測）

- 住所検索 `https://msearch.gsi.go.jp/address-search/AddressSearch?q=` — HTTP 200、
  `access-control-allow-origin: *`。APIキー不要。番地まで返ることもある（「鴻巣市本町」）。
- タイル `https://cyberjapandata.gsi.go.jp/xyz/{pale,std,seamlessphoto}/{z}/{x}/{y}` — 同じくCORS許可。
- 画面表示には出典表示が要るので MapLibre の attribution に「国土地理院」を入れている。

### ブラウザ操作で見つけた不具合

`new maplibregl.Marker().addTo(map)` を `setLngLat` より先に呼んでいた。
MapLibre が内部で未設定の lngLat を読んで落ちる。**住所を選んだ瞬間に画面が壊れる**
バグで、単体テストでは捕まらなかった。`scripts/e2e.mjs` で再発を検出できる。

### この環境の制約

コンテナのChromiumはプロキシ経由で外部に出られない（ERR_CONNECTION_RESET）。
そのため `scripts/e2e.mjs` は地理院への通信を stub する。実通信の確認は Node から
`scripts/gsicheck.ts` で行っている。**ブラウザで地理院タイルが実際に描画されることは
この環境では確認できていない。** 依頼者の環境での確認が要る。

---

## 2026-07-29 — 描画エンジン（参考図面への寄せ込み）

依頼者の指示「完成系は参考図面にできる限り近づける」を受け、GeoJSON取り込み（実データ待ち）
より先に描画そのものを作り込んだ。参考図面の構図を写真から起こしてサンプルにすれば、
実データが無くても描いて見比べられる。

### 記号（すべて参考図面の実物から読み取り）

| 記号 | 実物の見え方 | 実装 |
|---|---|---|
| 車 | 角の丸い車体＋内側にもう一周＋横切る4本 | 4.6m×1.78m のパラメトリック図形 |
| フェンス | 玉鎖線（線に小さな玉が連なる） | ピッチ1.1mm・玉半径0.32mm |
| 土留め | 境界から外向きのT字 | ピッチ4.5mm・軸1.5mm・横棒1.7mm |
| 隣家・法面 | ケバ（輪郭の片側に短い線） | ピッチ1.4mm・長さ1.0mm |
| 集水桝 | 小さな菱形 | 2.2mm |

**ケバの向きは多角形の回り方に依存する。** 頂点を時計回りに与えると左法線が外を向き、
建物のケバが外側に生えて櫛のようになる。符号付き面積で回り方を判定して吸収した。

**市道のケバは片側だけ。** 両側に長く出すと梯子のようになって実物と違う。

### 図郭でのクリップ

周辺の筆や道路は図郭の外まで続くので、クリップが無いと線が図枠を突き抜けて
紙いっぱいに走る。`Pen.clipped()` で W 演算子を1回だけ掛ける。
検証は `clipCount === 1`。

### 図枠は中身の後に描く

先に描くと、周辺の筆界線が表題枠を横切る。表題枠は白で塗りつぶし、中身の上に載せる。

### 参考図面との突合（検収基準6）

`npx vite-node scripts/compare.ts` で表を出す。現状 **一致17 / 相違4 / 未実装2**。

相違4件はいずれも意図的または軽微。
- 表題欄: 依頼者の指示で描かない
- 市道4→2、用悪水路3→2、集水桝1→2: サンプルの構図が実物と完全一致ではないため

未実装2件は第4段階（排水経路の矢印、進入口・隅切り）。

### 用紙の再確認

参考図面と同等の範囲（周辺込み 85.9×67.7m）は **A4に収まらない**。A3でも足りず
A2が必要という結果になった。敷地を斜めに置くと外接矩形が大きくなるため。
実データで再判定するが、A4で出すなら描く周辺をかなり絞る必要がある。

---

## 2026-07-29 — 図面規格の調査（憶測を典拠で置き換えた）

依頼者の指示「憶測ではなく図面のルールを調べて学べ」を受け、写真からの目測で決めていた
数値を規格・法令の値に置き換えた。典拠は `src/draw/standards.ts` に集約し、
規格値との一致をテストで固定した（`src/draw/standards.test.ts`）。

### 出典

- JIS Z 8311:1998 製図―製図用紙のサイズ及び図面の様式
- JIS Z 8312:1999 製図―表示の一般原則―線の基本原則
- 作業規程の準則 付録7 公共測量標準図式（国土地理院）
- 農地法関係事務処理要領（農林水産省）
- 駐車場法施行令第7条／駐車場設計・施工指針

### 判明したこと

**とじしろ20mm・左端は規格どおりだった。** JIS Z 8311 は「最小幅20mm（輪郭を含む）で、
表題欄から最も離れた左の端に置く」と定める。表題欄は右下隅なので、とじしろは左になる。
参考図面の綴じ穴が左端にあるのはこの規定の帰結であり、偶然ではない。

**輪郭線は最小0.5mm。** 図枠の外線を0.6mm→0.50mm（10号）に直した。

**中心マークは規格の要素だった。** 用紙四辺の中央から輪郭線の内側へ約5mm入るまで、
最小0.5mmで引く。図面の複写・折りたたみの基準。実装していなかったので追加した。

**線幅は数列から選ぶ。** 公共測量標準図式 第7条の線号は
1号0.05 / 2号0.10 / 3号0.15 / 4号0.20 / 5号0.25 / 6号0.30 / 7号0.35 / 8号0.40 / 10号0.50mm、
許容誤差±0.025mm。0.22mmや0.6mmのような中間値は規格に無い。全て線号に合わせた。
（JIS Z 8312 は 0.13/0.18/0.25/0.35/0.5/0.7/1/1.4/2、公比√2、極太:太:細=4:2:1。
この図面は地形図系なので公共測量標準図式の数列を採る。）

**記号の正式名称。** 公共測量標準図式の用語では次のとおり。
- 構囲（第30条）: 建物及び敷地等の周辺を区画する囲壁の類 ← 「フェンス」
- 法面（第29条）: 切土あるいは盛土によって人工的に作られた斜面 ← 「土留め」
- 被覆: 射影をもつ記号。上端は太線、下端は細線（第46条・第48条）
- 諸地（第31条）: 建物以外の土地。空地・**駐車場**・花壇・園庭・墓地・材料置場
- 水部（第26条）: 河川、細流、かれ川、**用水路**、湖池、海岸線、地下水路
- 側溝（U字溝・L字溝）は線号3＝0.15mm
- 記号及び文字の大きさの許容誤差は図上±0.2mm（第6条）

**駐車寸法の根拠。** 駐車マス2.5m×5.0m、車路は対面通行5.5m以上・一方通行3.5m以上。
既定値は正しかったが、5.5mが対面通行の下限であることを明示し、
基準を満たすかを `checkAisleWidth()` で判定できるようにした。

### 決定事項と食い違う2点（依頼者の判断が要る）

**(1) 縮尺。** 農地法関係事務処理要領は、この図面にあたる
「転用候補地に建設しようとする建物又は施設の面積、位置及び施設物間の距離を表示する図面」
の縮尺を **500分の1ないし2,000分の1程度** と定めている。CLAUDE.md の決定事項 1/250 は
この範囲より詳細側にある。より詳しい方向なので通常は差し支えないが、用紙の問題に直結する。

| 縮尺 | 要領の推奨 | A4図郭の実寸 | 参考図面相当(85.9×67.7m)が収まるか |
|---|---|---|---|
| 1/250 | × | 65.5×45.0m | 収まらない（A2が必要） |
| 1/300 | × | 78.6×54.0m | 収まらない（A3が必要） |
| **1/500** | **○** | **131.0×90.0m** | **A4に収まる** |
| 1/1000 | ○ | 262.0×180.0m | A4に収まる |

**1/500 にすると、要領の推奨範囲に入り、かつ依頼者のA4プリンタで等倍印刷できる。**
A4に収まらない問題の原因は1/250だった。

**(2) 施設物間の距離。** 同要領は上記図面に「**施設物間の距離**を表示する」ことを挙げている。
参考図面に寸法線は無いが、要領は距離の表示を求めている。CLAUDE.md の
「寸法線を入れない」は参考図面の実物に基づく決定であり、要領とは食い違う。

---

## 2026-07-29 — 対象地の実規模と縮尺1/250の妥当性

依頼者から対象地の面積が示された。**90坪（297.5㎡）と143坪（472.7㎡）。**
目的は「対象地の周辺を含む計画平面図を得ること」であり、1/250はそのための選択だった。

### 以前の報告を訂正する

「参考図面と同等の範囲はA4に収まらない」と報告したが、これは**参考図面の敷地を再現した
サンプル**での話で、依頼者の土地ではなかった。参考図面の敷地は約1337㎡＝**約404坪**あり、
依頼者の143坪の**2.8倍**の広さがある。

実測（`npx vite-node scripts/marginCheck.ts`）:

| 敷地 | 一辺 | 縮尺 | 用紙 | 敷地の外に描ける周辺（左右／上下） |
|---|---|---|---|---|
| 90坪 | 17.2m | 1/250 | A4 | 各 24.1m ／ 13.9m |
| 90坪 | 17.2m | 1/250 | A3 | 各 39.5m ／ 24.8m |
| 143坪 | 21.7m | 1/250 | A4 | 各 21.9m ／ 11.6m |
| 143坪 | 21.7m | 1/250 | A3 | 各 37.3m ／ 22.5m |

**1/250・A4で両方とも成立する。** 市道（5m）とその向こう側、用悪水路（2m）と隣接筆の
地番まで入る。縮尺を1/500に落とす必要はない。

### 注意点

敷地の**向き**が効く。A4横の図郭は 65.5m（東西）×45.0m（南北）で、南北が狭い。
敷地の長手が南北を向くと、上下に取れる周辺が急に減る。北固定なので図面を回して逃げられない。
実データで敷地の向きが分かった時点で再確認する。

`src/scenes/sample.ts` に対象地の規模のサンプルを置いた。
143坪・A4・1/250 で図面全体 57.2×37.2m、駐車16台。90坪で 52.7×34.7m、12台。

### 面積の食い違い

依頼者の示す90坪・143坪と、旧CLAUDE.mdの「約330㎡・約496㎡」（＝約100坪・約150坪）に
差がある。公簿と実測の別か概数かを確定させる必要がある。未決事項に追加した。

---

## 2026-07-29 — ゴールの動線をつないだ

依頼者から改めてゴールが示された。
**「住所を伝えて、ピンをずらして指定したところの計画平面図が欲しい。これがゴール。」**

それまで、地図（SitePicker）とPDF生成（buildPlanPdf）が**別々に動いていた**。
ピンの位置はPDFに渡っておらず、PDFは固定のサンプルを描いていた。ここをつないだ。

### つないだもの

- `originCenteredOn(point, paper, scale)` — 指した点が図郭の中央に来る原点を求める。
  図面の外接矩形ではなくピンそのものを紙の中心に据える。
- `makeSampleScene({ center, ... })` — 敷地の中心を引数で受け取る。ピンの位置がそのまま入る。
- `src/ui/PlanPanel.tsx` — 坪数・向き・地番・用紙を決めて出力するパネル。
  収まるかどうかをその場で表示する。

### ブラウザ操作での確認（`scripts/e2ePlan.mjs`）

1. 住所検索 → 選択　OK
2. 地図クリックでピン移動　OK（X=7348.975→8791.173、Y=-28023.951→-29163.456 と追従）
3. 「この場所の計画平面図PDFを出力」　OK（79,779バイト、先頭 `%PDF-`）
4. 収まり判定　「収まります（図面 57.2×37.2m ／ 図郭 65.5×45.0m）」

### 途中で直したもの

ダウンロードのアンカーをDOMに入れずにclickしていた。ブラウザによっては
`download` 属性が無視される。DOMに入れてからclickするよう直した（`src/ui/download.ts`）。

なお**ヘッドレスChromiumは日本語ファイル名を落として「download」にする**。
最小限のコードでも同じ挙動になることを確認済みで、実ブラウザでは日本語名がそのまま付く。
e2eスクリプトは名前ではなくPDFの中身で確かめるようにした。

### 残っている隔たり

敷地の形はいま矩形の仮置き。実際の筆界は登記所備付地図を取り込んでから確定する。
周辺（市道・用悪水路・隣接筆）も仮置きで、実データに置き換わる。
**動線としてはゴールに到達しており、あとは中身を実データにしていく段階。**

---

## 2026-07-29 — 登記所備付地図の実データを取り込んだ

### 入手

G空間情報センターのCKANで市区町村単位に公開されている。データセットIDは
`aigid-moj-{市区町村コード}`。APIでリソースURLが取れる。

    https://www.geospatial.jp/ckan/api/3/action/package_show?id=aigid-moj-11217

鴻巣市（11217）の2025年版GeoJSONは16MB、19,420筆。ファイル名は `11217__9_r_2025.geojson`。

### 実データの構造（憶測ではなく実物）

```json
"crs": { "type": "name", "properties": { "name": "urn:ogc:def:crs:OGC::CRS84" } }
"properties": {
  "ID": "H000000122", "市区町村C": "11217",
  "大字コード": "048", "丁目コード": "000", "小字コード": "0000", "予備コード": "00",
  "市区町村名": "鴻巣市", "大字名": "氷川町", "地番": "173",
  "精度区分": "甲二", "座標値種別": "測量成果",
  "地図名": "鴻巣市原馬室・滝馬室土地区画整理",
  "座標系": "公共座標9系", "測地系判別": "測量"
}
```

**座標は CRS84（経度緯度）。** 平面直角座標ではない。面積計算の前に必ず変換する。

**地目・面積・所有者はこのデータに無い。** 登記所備付地図が持つのは筆界と地番まで。
地目と所有者は登記事項証明書から、面積は座標法か公簿から入れる。画面で入力させている。

### 公図と地籍の判別子は「座標値種別」だった

鴻巣市 19,420筆の内訳。

| 属性 | 値の分布 |
|---|---|
| 座標値種別 | 図上測量 16,999 ／ 測量成果 2,420 ／ 空 1 |
| 精度区分 | 甲三 18,309 ／ 甲一 639 ／ 甲二 145 ／ 空 327 |
| 測地系判別 | 変換 17,000 ／ 測量 2,420 |
| 座標系 | 公共座標9系（全件） |

**「図上測量」が紙の図面を数値化したもの**にあたり、全体の88%を占める。
CLAUDE.mdが警告していた「公図由来の座標」はこれ。`isUsableForArea()` は
座標値種別が「測量成果」のときだけ true を返す。画面でも赤字で警告する。

「精度区分」は甲一〜甲三と細かいが、甲三でも座標値種別が図上測量なら精度は低い。
**判定は座標値種別で行う。**

### 収まり判定を申請地基準に変えた

周辺の筆は図郭の外まで続いていて当然で、図郭でクリップして端で切る前提。
これを収まり判定に混ぜると、実データではほぼ必ず「収まらない」になる
（実測で 573×365m と出た）。`subjectBoundsOf()` を足し、既定は申請地で判定する。

### 動線の確認（`scripts/e2eMoj.mjs`）

1. 住所検索 → ピン設置　OK
2. 地図データ読み込み　OK（269筆、鴻巣市、第IX系、測量成果269、甲一269）
3. ピンが指す筆を申請地に　OK（愛の町362-7、座標法120.3㎡＝36.4坪、周辺66筆）
4. PDF出力　OK（42,834バイト）

**ピンを指した場所の、実際の筆界と地番による計画平面図が出るようになった。**

---

## 2026-07-29 — 実データの筆形状への駐車区画の割り付け（`src/draw/layout.ts`）

これまで駐車区画は矩形のサンプル敷地にしか置けず、登記所備付地図から起こした
**実際の筆の形とつながっていなかった。** ここをつないだ。

### 敷地の向きの決め方

目測で決めない。**最小面積外接矩形**（凸包の各辺に沿って回す回転キャリパー法）で
敷地の向きを求める。実データの筆は正方形に近いことがあり長手方向が有利とは限らないので、
長手方向と、それに直交する向きの**両方を試して台数の多い方を採る。**

### 積み方

短手方向に「帯(5.0m) → 車路(5.5m) → 帯(5.0m)」の塊（15.5m）を積む。
塊どうしは**背中合わせに詰める。** それぞれの帯が自分の側の車路を向くので、
塊のあいだに車路を入れると5.5mまるごと無駄になる。
残りに「帯＋車路」だけ入るときは片側だけの列を足す。

**車路の付かない帯は作らない。** 車が出入りできない区画を台数に数えるのは嘘になる。
同じ理由で、車路の走る向きの長さが車路幅（5.5m）にも満たない場合は0台とする。
それは道ではなく行き止まりの窪みで、台数だけ増える無意味な配置になる。

### はみ出しの落とし方

マスの4隅すべてが筆の内側にあり、かつ**境界から離隔（既定0.5m）以上離れている**ことを
求める。内外判定だけだと辺にぴったり接するマスが通ってしまい、フェンス・土留めの厚みが入らない。
隅の欠けた実データの筆でも破綻しない。

### 一方通行の選択肢

駐車場法施行令第7条の車路幅の下限は対面5.5m・一方通行3.5m。実データの筆は細いことが多く、
11m角（121㎡）程度だと対面では1台も入らないが一方通行なら入る。
**既定は対面**とし、画面のチェックボックスで一方通行に切り替えられるようにした。
規格の値は `draw/standards.ts` から引く。ここで数字を書かない。

### ラベルの逃がし方

申請地のラベルは地番・地目・面積・所有者の4行になる。重心に置くと区画の上に重なって
読めなかった。`labelSpot()` で**筆の内側かつ区画からも境界からもいちばん離れた点**を
格子の総当たり＋局所探索で探し、そこに置く。

### 実測値（抜粋269筆・鴻巣市 愛の町）

| 筆 | 面積 | 外接矩形 | 対面5.5m | 一方通行3.5m |
|---|---|---|---|---|
| 327 | 666.9㎡ | 28.8×26.9m | 16台（41.7㎡/台） | 22台（30.3㎡/台） |
| 312 | 692㎡ | 49.0×14.1m | 25台 | — |
| 441 | 628㎡ | 30.6×20.5m | 17台 | — |
| 362-7 | 120.3㎡ | 13.6×9.0m | 0台 | 0台 |

362-7 が0台なのは正しい。短手9.0mから離隔1.0mを引くと8.0mで、
一方通行でも「帯5.0＋車路3.5＝8.5m」が入らない。画面にその旨と理由を出す。

参考図面は約1337㎡に23台＝58.1㎡/台。327の41.7㎡/台はそれより密で、過大ではない。

### 検証

`src/draw/layout.test.ts` 30件、`src/scenes/fromMoj.test.ts` に8件を追加（全242件）。
実データ60筆に総当たりして、**マスが筆の外に出ないこと**と
**マスの総面積が敷地面積を超えないこと**をテストで固定した。

---

## 2026-07-29 — 進入口と排水（第4段階、`src/draw/site.ts`）

### 分かったこと（これが本節の要点）

**登記所備付地図からは「どの辺が道路か」を判定できない。**

地目がデータに入っていないので、道路・水路の判別は地番の隣接関係から推定するしかない。
「隣に登記された筆が無い側が道路（無番地の里道・水路）」という推定を立てて実データで検証したが、
**成り立たなかった。**

| 筆 | 辺の数（1m以上） | 外側に隣接筆がある辺 |
|---|---|---|
| 327 | 7 | 7 |
| 441 | 6 | 6 |
| 312 | 6 | 6 |
| 362-7 | 6 | 6 |

道路も水路もすべて登記された筆として隣接していた（327の隣は 326・518・328・527・329・325）。
したがって**道路側と放流先は画面で選ばせる。** 既定値としていちばん長い辺を初期選択するが、
これは推定ではない。`src/draw/site.test.ts` にこの前提をテストとして固定してある
（成り立たないことを固定しているので、将来データが変わればテストが落ちて気づける）。

選択肢には**方位・辺長・隣接地番**を出す。「南向き・長さ9.0m・隣 362-8」のように出れば、
依頼者は図面と見比べてどれが道路か判断できる。

### 進入口と隅切り

- 開口は既定4.0m。**フェンスは開口部で切る。** 切らないと車の入れない図面になる。
  外周長が「周長 − 開口幅」になることをテストで固定した。
- 隅切りは開口の端から**敷地の内側**へ折る。最初は外側へ折っていて、
  敷地の外にフェンスが生えた。目視で気づいたのでテスト（隅切りの先端が敷地内）を足した。

### 排水

舗装すると雨水が浸透しなくなる。隣接農地への流入防止が審査上の最大の論点なので、
**矢印は必ず放流先へ向く**ことをテストで固定した（矢印の終点が集水桝で、
かつ放流先の辺までの距離が始点より短いこと）。

- 側溝は放流先の辺に平行、敷地の内側。線号は公共測量標準図式の3号（0.15mm）。
- 集水桝は側溝上に等間隔で2つ。**境界から1.0m内側。**
  最初は0.5mにしていたが、1/250では紙上2mmで、桝の菱形（2.2mm）が境界線をまたいだ。
- 参考図面には排水の矢印が無い（判別できず）。CLAUDE.md 第4段階の指示に従って入れている。
  参考図面の再現シーンには入れていないので、突合表では実データ図面のほうで数える。

### 検証

`src/draw/site.test.ts` 19件を追加（全262件）。ブラウザ動線（`scripts/e2eMoj.mjs`）に
辺の選択の確認を足し、住所検索→地図読み込み→筆の選択→放流先の選び直し→PDF出力まで
エラーなしで通した（45,314バイト）。

突合表は**一致17／相違6／未実装0**。相違6の内訳は、表題欄（依頼者の指示で描かない）、
市道・用悪水路の注記数（再現サンプルの作り込み不足）、集水桝の数、
排水矢印と進入口（参考図面に無いものを実データ図面で実装）。

### 残した穴

- 進入口から車路までの取り付け（車の通る道筋）は描いていない。開口と区画のあいだは
  空き地なので図面としては通るが、明示はしていない。
- 申請地が複数筆のとき、進入口と排水は**1筆目の外周**にしか付かない。
  複数筆を1つの敷地として統合する処理はまだ無い。

---

## 2026-07-29 — 依頼者から実座標を1点受領（33.2279338, 130.4150011）

Googleマップで取得した座標。**この座標での結論を先に書く。**

### 所在と系番号

| 項目 | 値 | 出所 |
|---|---|---|
| 所在 | 福岡県大川市 大字下林 | 地理院 逆ジオコーディングAPI（muniCd 40212） |
| 系番号 | 第II系（系番号2・EPSG:6670） | `estimateZone` が certain で判定。データ側の「座標系」欄も公共座標2系で一致 |
| 平面直角座標 | X(北) 25,429.340m / Y(東) −54,523.952m | |

系番号の推定は、住所なし（座標のみ）だと approximate、住所を与えると certain になった。
どちらも第II系で一致した。

### 登記所備付地図データの入手

G空間情報センターの `aigid-moj-40212`（福岡県-大川市）に2022〜2025年版のGeoJSONがある。
**ユーザー登録なし・CC-BY・無償**でダウンロードできた。2025年版 23.9MB。

- 読み込み **26,180筆**、読み飛ばし0。ブラウザでの読み込みは **2.0秒**。
- 座標値種別: 図上測量 8,074／測量成果 18,106
- 精度区分: 甲三 26,012／乙一 168

### この座標では計画平面図を作れない（2つの理由）

**理由1: ピンの下に筆が無く、周辺のデータが疎。**

| 半径 | 筆数 |
|---|---|
| 33m（A4図郭の半分） | 2 |
| 50m | 3 |
| 100m | 6 |
| 200m | 23 |

ピンは登記された筆の内側に入っておらず、いちばん近い筆界まで **14.0m**。
A4・1/250の図郭（65.5×45.0m）に入る筆が2〜3筆しかなく、図面はほぼ白紙になる。

**理由2: 周辺のすべてが公図由来で、求積に使えない。**

半径400m以内の71筆すべてが座標値種別「図上測量」＝公図（地図に準ずる図面）由来。
**求積に使える「測量成果」の筆は、いちばん近いものが2,775m先**（大字向島）。

大川市全体では測量成果18,106筆あるので、市内に14条地図の区域はある。
だが**この一帯は違う。** 以前「対象地は14条地図の区域」と決着させていたが、
少なくともこの座標については当てはまらない。CLAUDE.mdの
「公図由来の座標を求積に使わない」がそのまま効く場面。

### 周辺の筆（参考）

| 地番 | 面積 | 外接矩形 | 対面5.5m | 一方通行3.5m |
|---|---|---|---|---|
| 大字下林505 | 324.1㎡＝98.1坪 | 19.1×18.7m | 6台 | 6台 |
| 大字下林500-1 | 803.3㎡＝243.0坪 | 167.4×32.6m | 0台 | 0台 |
| 大字下林499 | 153.0㎡＝46.3坪 | 26.6×9.6m | 0台 | 2台 |
| 大字下林500-3 | 73.3㎡＝22.2坪 | 22.6×5.1m | 0台 | 0台 |

500-1 は外接167×33mの細長い形で、道路か水路の筆と思われる。

**505 の 324.1㎡＝98.1坪は、旧CLAUDE.mdの「約330㎡」に近い。**
依頼者の言う「90坪」と旧記載「約330㎡（約100坪）」の食い違い（未決事項）は、
実測値が98.1坪であることを踏まえると旧記載のほうが近い可能性がある。ただし
この筆が対象地だという確証は無い。ピンから17.2mずれている。

### 割り付けの向きを総当たりにした（この検証で見つけた改善）

外接矩形の長手・短手の2通りだけ試していたが、実データで取りこぼしていた。
5度刻みで総当たりに変え、同数なら外接矩形の向きを優先する。

同時に**欠陥が1つ出た。** 斜めに振ると局所の外接範囲は広くなるので、
「外接範囲には入るが敷地には入らない車路」が通ってしまう。6×40mの細長い敷地に
45度で3台入るという誤った結果が出た。車路を幅×長さの帯として敷地内外判定するよう直した。

| 筆 | 直す前 | 総当たり（欠陥あり） | 総当たり＋車路判定 |
|---|---|---|---|
| 鴻巣327 | 16台 | 22台 | **18台** |
| 鴻巣441 | 17台 | 20台 | **17台** |
| 大川499（対面） | 2台 | — | **0台** |

総当たりだけでは22台/20台と出たが、そのうちは入れない車路を含んでいた。
車路判定を入れた18台/17台が正しい値。大川499も2台→0台になり、より正確になった。

実データ300筆で総当たりして、**筆の外に出たマスは0**、所要1.9秒（約6ms/筆）。

### この座標での成果物

- ピン中心のA4: 35,735バイト。ほぼ白紙（前述の理由1）。
- 505中心のA4: 55,652バイト。6台・進入口1・放流先1・桝2・矢印1・通路1。図面として成立。
- ブラウザ動線（26,180筆の実ファイル）: 住所検索→読み込み2.0秒→筆の選択→放流先の選び直し→
  PDF出力までエラーなし。**公図由来の警告が画面に赤字で出ることも確認。**

### 依頼者に確認すべきこと

1. この座標は対象地（90坪・143坪）のどちらかか。ピンの下に筆が無いので、
   もう少し正確な位置か、**地番**を教えてもらうのが確実。
2. 求積は地積測量図の実測値で確定する必要がある（この区域は公図）。地積測量図はあるか。
