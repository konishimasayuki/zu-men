# zu-men — 開発方針

土地利用計画平面図（計画平面図）作成アプリ。

## 目的

農地法5条（転用・権利移動）の許可申請に添付する計画平面図を、縮尺1/250で作図しPDFで出力するWebアプリ。用途は、購入予定の土地を自社の駐車場に転用する申請。提出先は市役所（農業委員会）。今後も自社で繰り返し使う。

完成イメージは `reference/` に置いた既存図面の写真を正とする。実装中は常にこの図面と見比べること。

## 依頼者の前提

依頼者はコードを書かない。実装上の判断はClaude Codeが行うが、成果物の正否を依頼者が読んで判断することはできない。したがって「動きました」という報告は受け付けない。本書末尾の検収基準について、実測値を添えて報告すること。

## 参考図面から読み取った要件

`reference/IMG_9055.png` は撮影時に90°回転している。正しい向きは**A3横**、綴じ穴は紙の左端。2026-07-29に向きを補正して各部を拡大確認し、本節を実物に合わせて書き直した。

紙面の6〜7割を周辺が占める。隣接地番、複数の市道、用悪水路、隣家。申請地の駐車場そのものは3割程度。**周辺の描画がこの図面の本体である。** 付け足しの機能として扱わない。

### 内容

- 申請地は複数の筆で構成される（参考図面では2筆）。単一地番を前提にした設計にしない。
- 申請地には地番・地目・面積・所有者名を記入する。隣接地は地番のみ（参考図面では 32・89・91）。
- 車は矩形ではなく平面シンボル（車の輪郭）で描く。参考図面では約23台。
- 敷地外周にフェンス（玉鎖線）と土留め（外向きT字記号）、区画間に「通路」の文字。
- 用悪水路が複数辺に接している。排水の放流先であり、審査上の要点。
- **この図面に求積表はない。** 求積は別紙とする。
- **寸法線はない。** 台数の記載もない。どちらも入れない。
- 進入口は市道側に手書きの引出し線が1箇所あるのみ。明確な矢印記号は判別できなかった。

### 体裁（実物に基づく。ここを勝手に変えない）

| 要素 | 位置・形式 |
|---|---|
| 図枠 | 二重線 |
| 表題「計画平面図　S＝1/250」 | 上辺中央やや右、**横書き**の枠囲み |
| 方位記号 | **左上**。八芒星＋矢羽根、北はほぼ真上 |
| 綴じ代 | **左端**。参考図面の2穴は紙のパンチ穴であり作図要素ではない |
| 表題欄 | 右下に枠が1つ。参考図面では空欄。**本アプリでは描かない**（依頼者の指示） |
| 出典表示 | **なし。** 参考図面は地図画像を一切使わない純粋な線画 |
| 凡例 | なし |

## 図面規格の典拠

数値は憶測で決めない。根拠のある値だけを `src/draw/standards.ts` に置き、
規格値との一致をテストで固定する。

| 対象 | 典拠 |
|---|---|
| 用紙サイズ・とじしろ・輪郭線・中心マーク・表題欄 | JIS Z 8311:1998 |
| 線の太さの標準数列 | JIS Z 8312:1999 |
| 線号・記号・用語（構囲/法面/被覆/諸地/水部） | 作業規程の準則 付録7 公共測量標準図式 |
| 添付図面の種類・縮尺・記載事項 | 農地法関係事務処理要領（農林水産省） |
| 駐車マス・車路幅員 | 駐車場法施行令第7条／駐車場設計・施工指針 |

線幅は公共測量標準図式 第7条の線号（0.05〜0.50mmの9段階）に必ず一致させる。
中間値は使わない。

## 決定事項（変更しない）

| 項目 | 決定 |
|---|---|
| 縮尺 | 1/250 固定 |
| 方位 | 北固定。図面の回転機能は作らない |
| 用紙 | **A4横を既定**（図郭 262×180mm ＝ 実寸 65.5m×45.0m）。依頼者のプリンタがA4のため。A3/A2/A1も選べる |
| 対象 | 約330㎡と約496㎡の2件。離れた土地であり、1プロジェクト＝1敷地＝1図面 |
| 座標系 | JGD2011 平面直角座標系（EPSG:6669〜6687）。系番号は住所検索の座標から推定し、画面で確認させる |
| 出力する図面 | **計画平面図のみ。** 求積図・求積表は当面作らない |
| 面積計算 | 座標法。計画平面図に記入する筆ごとの面積に使う |
| 表題欄 | **描かない。** 会社名等の記入欄は不要と依頼者が判断した |

## 技術構成

- Vite + React + TypeScript
- 地図表示: MapLibre GL JS
- 背景タイル: 国土地理院タイル（標準地図・航空写真）。**画面編集の下敷き専用**
- 住所検索: 国土地理院 住所検索API。APIキー不要・無償。検索結果の緯度経度から系番号を推定する
- 筆界・地番データ: 法務省 登記所備付地図データ（下記）
- 座標変換: proj4js
- PDF生成: pdf-lib（単位はpt。1mm = 2.834645669pt）
- 印刷補正: 用紙中心を固定した拡大縮小を、ページ内容の先頭に `cm` 演算子1つとしてだけ置く
- 日本語フォント: @pdf-lib/fontkit でサブセット埋め込み。Noto Sans JP または IPAexゴシック（いずれも埋め込み可のライセンス）
- ホスティング: GitHub + Vercel

**PDFは純粋なベクタ線画とし、地図画像を焼き込まない。** 参考図面がそうなっているため。地理院タイルは画面上で筆界を確認するための下敷きにのみ使い、出力には一切載せない。この方針により図面内の出典表示は不要になる。

Googleマップの地図画像は図面の背景にも画面の下敷きにも使わない。印刷・加工に規約上の制約があるため。Google Geocoding APIも使わない。取得した座標をGoogle以外の地図に表示することが規約で制限されており、ブラウザに置くAPIキーの課金・悪用リスクもある。**「住所を検索して地図で場所を指定する」機能自体は要件であり、地理院の住所検索APIと地理院タイルで実現する。** 地理院タイルの利用規約は着手前に最新版を確認すること。

日本語フォントの埋め込みは第0段階で必ず通す。pdf-lib の標準フォントに日本語はなく、所有者名に任意の漢字が来るためビルド時の事前サブセットが効かない。フォント実体（4〜8MB）をアセットとして同梱し、実行時にサブセットする。ここを後回しにすると第2段階の終盤で作り直しになる。

## 登記所備付地図データの扱い

周辺の筆界と地番を手作業で描かせないための中核データ。法務省がG空間情報センターで無償公開している。

- 原本は地図XML形式だが、G空間情報センターが変換済みのGeoJSON形式・シェープファイル形式も提供している。**GeoJSONはユーザー登録なしでダウンロードできる**ため、これを使う。XMLパーサは書かない。
- 市区町村単位のzipで配布される。アプリはファイルを読み込む方式とし、実行時にサーバから取得しにいかない。
- 取り込んだ筆から申請地の地番を選択すると、敷地ポリゴンと隣接筆が同時に確定する設計にする。

**精度に関する重大な注意。** このデータには地籍調査済みの14条地図と、精度の低い公図（地図に準ずる図面）が混在する。公図区域のデータは任意座標系で形状も歪んでおり、周辺の描画には使えるが**求積には使えない**。取り込み時に区分を判定して画面に表示し、公図由来の筆を求積の対象にしようとした場合は警告すること。求積は必ず地積測量図の実測値で確定する。

## 絶対に守る制約

1. **ブラウザ印刷で図面を出力しない。** `window.print()` や `@page` はプリンタ側の余白補正・縮小により縮尺が数％狂う。出力は必ずpdf-libで生成したベクタPDFのダウンロードとする。
2. **印刷補正を縮尺の代わりに使わない。** プリンタの機構誤差を吸収する印刷補正は用意するが、許容範囲は±5%に限る。それを超える食い違いは設定ミス（「用紙に合わせる」が有効等）であり、補正で隠さず原因を直す。補正を有効にしたPDFはデータとしては1/250でなくなるため、電子提出には使わない。
3. **縮小印刷で用紙に収めない。** A3で作図した図面をA4に縮小印刷すれば縮尺は250分の1でなくなる。用紙に収まらないときは、より大きい用紙で等倍印刷するか作図範囲を狭めるかの二択であり、縮小印刷は選択肢に入れない。
4. **画面の表示倍率と出力縮尺を連動させない。** 画面は編集用のプレビューであり、縮尺の真実はPDF生成コードだけが持つ。ズーム操作が出力に影響してはならない。
5. **面積計算を緯度経度のまま行わない。** 必ず平面直角座標に変換してから座標法で計算する。
6. **公図由来の座標を求積に使わない。** 前節のとおり。

## 機能要件

下記の順に実装する。各段階で検収基準を通してから次に進む。

### 第0段階 — 縮尺とフォントの実証

最大のリスクを先に潰すための薄い段階。ここを通るまで他の機能を書かない。

- 20.000m×20.000mの正方形を1つハードコードし、A4横のPDFを出力する。
- 図枠（二重線）、日本語の表題枠、方位記号まで入れる。右下の表題欄は描かない。
- 100mmと200mmの基準線を入れた検証用PDFを別に出力する。
- `paper/transform.ts` に縮尺変換を1箇所だけ定義し、PDF生成層以外から参照させない。
- 印刷補正を実装する。実測値を入力すると倍率を計算し、ブラウザに保存して次回も効かせる。

### 第1段階 — 地図データの取り込みと敷地の確定

- 住所を検索して地図ビューを対象地へ移動する。検索結果の座標から系番号を推定し、画面に出して確認させる。
- 登記所備付地図のGeoJSONを読み込み、筆界と地番を地図上に表示する。
- 14条地図と公図の区分を判定して表示する。
- 申請地の地番を選択して敷地とする。複数筆を選択でき、筆ごとに地目・面積・所有者を保持する。
- 各辺の辺長を手入力して上書きできる。地積測量図の値を正とする。
- 閉合誤差を計算して表示。許容範囲内なら座標を調整して確定する。
- データが無い区域のフォールバックとして、地図上のクリックによる概形作図も残す。
- プロジェクトのJSONエクスポートとインポートを実装する。

### 第2段階 — PDF出力（計画平面図）

- A4横、縮尺1/250。体裁は「参考図面から読み取った要件」の体裁表に従う。
- 用紙枠を敷地の上でドラッグして配置を決めるUI。地図をズームするのではなく紙を動かす。
- 申請地・隣接地の地番ラベル、道路名、水路名を配置し、ラベル位置は手で動かせる。
- 敷地が用紙に収まらない場合は警告し、A3（96.25m×66.75m）・A2（139.75m×97.5m）・A1（200.25m×139.75m）の順で提案する。**縮小印刷は提案しない。**
- 図枠・表題は敷地データから分離したテンプレートとして持つ。2件で同じ体裁を使い回すため。

### 第3段階 — 駐車区画の自動割付

- 標準区画 2.5m×5.0m、車路幅 5.5m（対面通行の下限）を既定値とし、変更できるようにする。一方通行は3.5m以上。駐車場法施行令第7条による。
- 敷地形状と出入口の位置から区画を自動配置し、手で微調整できる。
- 車は平面シンボルで描画する。参考図面の輪郭に合わせたSVGパスをアセットとして持つ。
- 台数は画面上で集計して表示する。**図面には記載しない**（参考図面に記載がないため）。

### 第4段階 — 付帯施設と排水

- 進入口、隅切り、フェンス・土留めの記号。
- 雨水の排水経路と放流先を矢印で表現する。舗装により浸透しなくなるため、隣接農地への流入防止が審査上の最大の論点。
- 集水桝・側溝の記号。

### 当面やらないこと

求積図と求積表は作らない。依頼者の指示により、いまは計画平面図だけを完成させる。
ただし面積計算そのものは計画平面図に筆ごとの面積を記入するために必要なので、座標法の実装とテストは残す。

## 検収基準

完了を宣言する前に以下をすべて実測し、結果の数値を報告する。実行していない項目は「未検証」と書くこと。推測で「問題ない」と書かない。

1. **縮尺の実測** — 二段構えで確認する。
   - Claude Code側: 生成PDFのコンテンツストリームを解析し、20mの辺が226.772pt（±0.01）であることを自動テストで確認する。
   - 依頼者側: プリンタ設定「実際のサイズ（100%）」でA4に印刷し、定規で測る。20mの辺が80.0±0.5mmであること。**この実測はClaude Codeには代行できない。** 依頼者の実測結果が出るまで、この項目は「未検証」と書く。
   - ずれていた場合: 実測値を印刷補正に入力し、補正後のシートを刷り直して80.0±0.5mmに入ることを確認する。
2. **面積計算の検証** — 20.000m×20.000mの正方形を座標法で計算し、400.00㎡になること。
3. **座標変換の検証** — 既知の緯度経度と平面直角座標の対応をユニットテストで固定する。
4. **閉合誤差** — 意図的に閉合しない辺長を入力し、誤差が数値で表示され、無警告で通過しないこと。
5. **地図データの検証** — 実際の対象市町村のGeoJSONを読み込み、既知の地番が正しい位置に表示されることを目視確認する。読み込んだ筆数を報告する。
6. **参考図面との照合** — 出力PDFと `reference/` の図面を並べ、記載要素の過不足を一覧にして報告する。

## デプロイ

Vercelにデプロイすると既定で公開URLになる。取得予定地の所在地・面積・所有者情報が載るため、**Deployment Protection（パスワードまたはVercel認証）を必ず有効にする。** 有効化した事実を報告に含めること。

## やらないこと

- Googleマップの地図画像を使う。Google Geocoding APIを使う。
- PDFに地図画像（地理院タイル・航空写真）を焼き込む。
- 求積図・求積表を作る（当面）。
- 寸法線を入れる。台数を図面に記載する。右下に表題欄（会社名等の記入欄）を描く。
- 図面の回転。
- 実測に代わる精度の主張。本アプリの座標は地積測量図の値で確定するものであり、地図データやクリック座標を測量成果として扱わない。この旨をアプリのUI上で明示する。

## 未決事項

### 残っているもの

- **検収基準5に使う実データ。** 対象市町村の登記所備付地図GeoJSONを1本、いずれ入手する必要がある。設計はブロックしない。
- **縮尺を1/250のままにするか。** 農地法関係事務処理要領は、この図面の縮尺を **1/500〜1/2,000程度** と定めている。1/250はこれより詳細側で、通常は差し支えないが、A4に収まらない原因でもある。**1/500にすると要領の推奨範囲に入り、かつA4に等倍で収まる**（A4図郭 131.0m×90.0m）。1/250は決定事項なので依頼者の判断を仰ぐ。
- **施設物間の距離を記載するか。** 同要領は「施設の面積、位置及び**施設物間の距離**を表示する図面」としている。参考図面に寸法線は無く、CLAUDE.mdも「寸法線を入れない」と決めているが、要領とは食い違う。
- **A4に周辺がどこまで入るか。** A4の図郭は実寸 65.5m×45.0m。約496㎡（22m四方）の敷地なら左右に約21m、上下に約11mしか周辺を描く余地がない。参考図面は約96m×66mの範囲を描いている。第2段階で実データを入れたときに周辺が不足するようなら、(1)描く周辺を絞る (2)A3で出してコンビニ等で等倍印刷する (3)縮尺を変える（1/250は決定事項なので要相談）の三択になる。

### 決着済み（2026-07-29、3回目のやりとり）

- 用紙の既定は**A4横**。依頼者のプリンタがA4。A3にはこだわらない。
- **右下の表題欄は描かない。** 会社名等の記入欄は不要。
- 依頼者はいま印刷できる環境にないため、検収基準1は当面「未検証」のまま。**印刷補正（キャリブレーション）を実装し、あとから実測値を入れて調整できるようにした。**

### 決着済み（2026-07-29、2回目のやりとり）

- 所在市町村はアプリ側で解決する。**住所検索と地図ビューを実装し、系番号は座標から推定して画面で確認させる。** Googleマップは使わず地理院で代替する。
- **いま必要なのは計画平面図のみ。** 求積図・求積表は作らない。農業委員会への様式確認も当面行わない。

### 決着済み（2026-07-29）

- 対象地は**14条地図の区域**。ただし求積は地積測量図の実測値で確定する方針を維持し、地図データは周辺描画に留める。公図判定と警告の機能は将来の別案件のために残す。
- 図面の体裁は**参考図面の実物に合わせる**。CLAUDE.mdの旧記述（右端縦書きの表題、右上の方位記号、上辺の綴じ代）は回転した写真の誤読だったため訂正済み。
- PDFは**ベクタ線画のみ**。地図画像は焼き込まない。出典表示は不要。
- **寸法線は入れない。** 参考図面に無いことを実物で確認した。
- 実装順序は**第0段階（縮尺・フォントの実証）を先に置く**。
